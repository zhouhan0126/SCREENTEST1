!!! 5
%html

  %head
    %meta(charset="utf-8")
    %title Welcome to Weread

    %link(href="/static/css/bootstrap.css", rel="stylesheet")
    %link(href="/static/css/screenly.css", rel="stylesheet")

  %body.splash

    .container
      .row
        .span12
          .logo-container.page-header.text-center
            %img(src="/static/img/screenly_ose_logo_large.png")

      .row
        .span12.text-center
          - if ip_lookup:
            %p.lead 通过访问以下网址，管理播放资源等设置:

            .well.lead
              %a(href="${msg}") ${msg}
          - else:
            %p.lead ${msg}

      %hr
